<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.wgn.website.mapper.BiliolMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.wgn.website.entity.BiliolEntity">
    <result column="id" property="id" />
    <result column="create_time" property="createTime" />
        <result column="ranking" property="ranking" />
        <result column="title" property="title" />
        <result column="author" property="author" />
        <result column="uid" property="uid" />
        <result column="link" property="link" />
        <result column="play_num" property="playNum" />
        <result column="dm_num" property="dmNum" />
        <result column="ol_num" property="olNum" />
        <result column="create_time" property="createTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        create_time,
        ranking, title, author, uid, link, play_num, dm_num, ol_num, create_time
    </sql>

</mapper>
